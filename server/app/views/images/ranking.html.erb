<style>
  li {
    padding:6px;
    width:300px;
    list-style:none;
  }
</style>
<ul>
<% @images.each do |image| %>
  <li><%= image.cnt %> voted:<br/><%=link_to image_tag(image.path), :controller => :images, :action => :show, :id => image.id %></li>
<% end %>
</ul>
